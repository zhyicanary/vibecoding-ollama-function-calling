# AI Agent 数字人 — 项目技术文档

## 一、项目概述

一个**本地运行的智能数字人助手**，支持多语言对话、工具调用（天气/股票/邮件/钉钉）、RAG 课程问答、语音合成（TTS）和语音输入。基于 **LangGraph** 构建 Agent 流程，使用 **Ollama** 加载本地大模型，无需联网即可运行。

---

## 二、技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| **后端框架** | Flask 3.x | Web 服务 |
| **AI 框架** | LangChain 1.2.15 + LangGraph 0.2+ | LLM 调用、Agent 编排 |
| **大模型** | Ollama（qwen3:0.6b / qwen3:8b / deepseek-r1:7b） | 本地推理 |
| **向量数据库** | Chroma + OllamaEmbeddings | RAG 知识库检索 |
| **混合检索** | EnsembleRetriever（向量 50% + BM25 50%） | 提高检索精度 |
| **语音合成** | edge-tts（微软 Edge TTS） | 多语言 TTS |
| **语音输入** | Web Speech API | 浏览器语音识别 |
| **前端** | 原生 HTML/CSS/JS | 交互界面 |

---

## 三、项目结构

```
ai-agent/
├── app.py                     # 主入口：Flask 服务 + LangGraph Agent + RAG
├── tools.py                   # 工具函数（天气/股票/时间/邮件/钉钉）
├── requirements.txt           # Python 依赖
├── .env.example               # 环境变量模板（SMTP/Ollama/TTS）
├── 《智能应用系统设计》课程介绍.md  # RAG 知识库源文件
├── langgraph_ai-agent.png     # LangGraph 流程可视化图
├── public/                    # 前端静态文件
│   ├── index.html             # 主页面
│   ├── app.js                 # 前端逻辑（聊天/TTS/语音输入）
│   └── styles.css             # 样式
├── resources/                 # 静态资源（如头像图片）
└── langgraph_ai-agent.png     # 生成的流程图
```

---

## 四、核心架构设计

### 4.1 整体流程

```
用户输入
   │
   ▼
┌─────────────┐
│  Router Node │ ← LLM 判断意图：chat/time/weather/stock/email/dingtalk/course_rag
└──────┬──────┘
       │
       ▼ (条件边，根据 route 值分流)
       │
       ├──→ chat          → 直接 LLM 对话
       ├──→ time          → 调用 get_time 工具
       ├──→ weather       → 提取城市名 → 调用 get_weather
       ├──→ stock         → 提取股票代码 → 调用 get_stock_price
       ├──→ email         → 提取参数 → 调用 send_email_tool
       ├──→ dingtalk      → 提取消息内容 → 调用 send_dingtalk
       └──→ course_rag    → RAG 检索课程知识库回答
                │
                ▼
           返回 reply
```

### 4.2 LangGraph 状态图

用 `StateGraph(RouterState)` 构建，状态类型定义：

```python
class RouterState(TypedDict, total=False):
    input: str       # 用户输入
    model: str       # 模型名称
    language: str    # 语言
    route: str       # 路由：chat/time/weather/stock/email/dingtalk/course_rag
    reply: str       # 最终回复
```

**节点：**
- `router` — 用 LLM 判断用户意图，输出 route
- `chat` — 纯对话，不调用工具
- `time/weather/stock/email/dingtalk` — 各工具节点（参数提取 + 调用 + 回复生成）
- `course_rag` — RAG 检索节点

**边：**
- `START → router`（固定）
- `router → chat/time/weather/...`（条件边，根据 route 值选择）

### 4.3 路由与条件分支

```python
workflow.add_conditional_edges(
    "router",
    route_next,           # 从 state["route"] 取值
    {                     # route → 节点映射
        "chat": "chat",
        "time": "time",
        "weather": "weather",
        "stock": "stock",
        "email": "email",
        "dingtalk": "dingtalk",
        "course_rag": "course_rag",
    },
)
```

如果 LLM 返回的 JSON 解析失败，还有关键词兜底 `fallback_route()`。

### 4.4 工具节点生成器

```python
def build_tool_node(tool_fn, arg_prompt=None, arg_names=None):
    def node(state: RouterState) -> RouterState:
        # 1. 调用 LLM 从用户输入中提取参数（如城市名、股票代码）
        # 2. 调用对应的工具函数
        # 3. 用 LLM 将工具结果转成自然语言回复
        return {"reply": reply}
    return node
```

每个工具节点三步走：**提取参数 → 调用工具 → 润色回复**。

---

## 五、RAG 知识库系统

### 5.1 流程

```
课程介绍.md
    │ MarkdownHeaderTextSplitter（按 #/##/### 切块）
    ▼
文档块
    │ RecursiveCharacterTextSplitter（chunk=500, overlap=50）
    ▼
文档切片
    │
    ├──→ Chroma（向量检索）
    │         ↑ OllamaEmbeddings
    │
    ├──→ BM25Retriever（关键词检索）
    │
    └──→ EnsembleRetriever（权重各 0.5）
                │
                ▼
          检索结果 → LLM 生成回答
```

### 5.2 混合检索优势

- **向量检索**：语义匹配，适合"课程培养目标是什么？"这种问法
- **BM25**：关键词匹配，适合"CW3024"、"郑耀东"这种精确查询
- **Ensemble**：两者加权综合，互补短板

### 5.3 问答规则

- 严格基于检索到的资料回答
- 资料中没有答案则回复"根据已有资料无法确认"
- 禁止编造

---

## 六、工具系统

| 工具 | 功能 | 数据源 |
|------|------|--------|
| `get_time` | 当前时间（日期、星期、时间） | 本地系统时间 |
| `get_weather` | 城市天气（温度、湿度、天气状况） | sojson 天气 API |
| `get_stock_price` | A 股实时股价（价格、涨跌幅） | 新浪财经 API |
| `send_email_tool` | 发送邮件 | SMTP（需配置） |
| `send_dingtalk` | 钉钉群消息 | 钉钉 Webhook |
| `course_rag_question` | 课程知识问答 | RAG 检索 |

所有工具函数的底层实现在 `tools.py` 中，`app.py` 中用 `@tool` 装饰器包装为 LangChain Tool。

---

## 七、多语言系统

支持 **14 种语言**的对话和 TTS：

| 地区 | 语言 | TTS 音色 |
|------|------|----------|
| 简体中文 | zh-CN | zh-CN-XiaoxiaoNeural |
| 粤语 | zh-HK | zh-HK-HiuGaaiNeural |
| 台湾国语 | zh-TW | zh-TW-HsiaoYuNeural |
| 美式英语 | en-US | en-US-JennyNeural |
| 日语 | ja-JP | ja-JP-NanamiNeural |
| 韩语 | ko-KR | ko-KR-SunHiNeural |
| ... | ... | ... |

系统提示词根据语言自动切换，包括语气和文化适配。

---

## 八、Flask API 接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/chat` | 聊天入口，接收 `{model, language, messages}` |
| POST | `/api/tts` | 文本转语音，返回 MP3 |
| GET | `/api/tts-status` | 查询 TTS 状态 |
| GET | `/` | 前端页面 |
| GET | `/resources/<path>` | 静态资源 |
| GET | `/<path>` | 前端静态文件 |

### `/api/chat` 请求体

```json
{
  "model": "qwen3:0.6b",
  "language": "zh-CN",
  "messages": [
    {"role": "user", "content": "今天北京天气怎么样？"}
  ]
}
```

### 响应

```json
{
  "reply": "北京今天气温25°C，晴，适合出门。"
}
```

---

## 九、前端概要

原生 JS 实现，无框架依赖，核心功能：

- **聊天界面**：消息列表、滚动、气泡样式
- **模型选择**：下拉切换 Ollama 模型
- **语言选择**：14 种语言即时切换
- **TTS 开关**：控制是否语音播报
- **语音输入**：浏览器 Web Speech API 识别
- **数字人状态**：待机/思考/说话动画
- **消息管理**：最多存 24 条，上下文取最近 12 条

---

## 十、运行方式

```bash
# 1. 确保 Ollama 运行中且有模型
ollama serve

# 2. 安装依赖
uv pip install -r requirements.txt

# 3. 配置环境变量（可选）
cp .env.example .env
# 编辑 .env 填入 SMTP 等配置

# 4. 启动
uv run python app.py

# 5. 浏览器打开
http://127.0.0.1:3000
```

---

## 十一、架构特点总结

| 特性 | 实现方式 |
|------|----------|
| **Agent 模式** | LangGraph StateGraph + Router 条件分支 |
| **状态管理** | RouterState（TypedDict），节点间共享 |
| **意图识别** | LLM 路由 + 关键词兜底 |
| **工具调用** | 先 LLM 提参 → 调工具 → LLM 润色回复 |
| **知识库** | RAG（向量 + BM25 混合检索） |
| **记忆** | 前端传历史消息（无后端持久化） |
| **可扩展** | 加新工具只需：实现函数 + @tool 装饰 + 加节点和边 |
